#!/sbin/runscript

depend() {
	need localmount
	after logger
}

start() {
	echo "Starting Eisfair Installation ..."
	sleep 1
	/bin/eis-install
	[ -f /reboot ] && reboot
}

stop() {
	echo ""
	exit 0
}
