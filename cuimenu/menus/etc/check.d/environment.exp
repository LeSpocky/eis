#-----------------------------------------------------------------------------
# /etc/check.d/environment.exp - expressions for eischk
#-----------------------------------------------------------------------------
ENVIRONMENT_PROXY  = '()|((http://)?((RE:FQDN)|(RE:IPADDR)):(RE:PORT))(/)?'
                   : 'Invalid proxy entry, use server:port'
