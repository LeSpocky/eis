#!/bin/sh

/var/install/bin/add-menu setup.services.menu setup.services.dhcp.menu "DHCP Administration"
#/var/install/bin/add-menu --logfile setup.system.logfileview.menu "/var/log/skeleton.log" "Show Skeleton Log"

[ -e /etc/config.d/dhcp ] || cp /etc/default.d/dhcp /etc/config.d/
exit 0
